---
name: search-long-term-memory-skill
description: 此技能用于检索长期记忆，从跨会话长期记忆中补充人物、事件、知识或经验信息。
---

# 长期记忆检索技能

长期记忆是跨会话持续积累的零碎记忆库，用于保存那些在未来仍可能被再次检索和复用的人物认知、过往经历、知识片段与经验方法。它不会自动注入当前上下文，需要主动检索后再使用。

长期记忆包含 `过往事件`、`人物画像`、`百科知识`、`经验方法` 4 个一级分类，每个一级分类下有若干二级分类。

1. 过往事件

- 用于记录具体经历，通常包含人物、场景、过程、结果或心理活动
- 二级分类：
  - `owner`：与 owner 相关的事件
  - `other`：与其他人相关的事件
  - `self`：与“我”相关的具体经历或见闻，但它仍然是事件，不是人格自述

2. 人物画像

- 用于记录对人物的稳定认知，例如身份、关系、长期偏好、习惯、边界、长期特征，不适合写一次性的情绪波动或某一天的临时状态
- 二级分类：
  - `owner`：owner 的画像
  - `other`：其他人的画像

3. 百科知识

- 用于记录可脱离具体会话复用的客观知识，包括常识、术语、作品设定、梗来源、背景资料等
- 二级分类：
  - `文史`
  - `理工`
  - `生活`
  - `娱乐`
  - `梗知识`
  - `其他`

### 经验方法

- 用于记录可复用的理解方式、应对策略和做法，重点是“在某类情境下，怎样做更合适”
- 二级分类：
  - `general`

---

## 工具行为

1. 一次调用可以提交多个 query，参数为 `queries`，每个 query 都是一个独立检索单元，只负责一个明确的检索目标
2. 检索结果按查询分组返回，每组结果返回的是按相关性排序的候选提示 `hints`，但结果中仍可能存在噪声或弱相关项，需要自行鉴别和取舍
3. 某些 hint 可能带有 `msg_ids`，表示这条长期记忆关联的来源消息 ID，可用于后续追溯原始依据
4. 需要精确原文、消息时间线、图片或文件内容时，应使用技能 `query-chat-history-skill`

---

## 执行流程

1. 根据系统给出的检索要求，拆分本次需要检索的目标
2. 每个目标单独写成一个 query，不要把多个目标混在同一个 query 里
3. 为每个 query 选择合适的检索参数，并用自然语言描述要检索的长期记忆内容
4. 调用 `exec_skill` 执行 `search-long-term-memory-skill`

---

## Query 参数说明与检索指南

1. `memory`
   - 用自然语言描述要找什么，不要只堆关键词
   - 一个 query 只写一个检索目标；如果要查多个目标，就拆成多个 query
2. `index0`
   - 表示一级分类，必填
3. `index1`
   - 表示二级分类，可选
   - 宽泛检索时可以不指定，填空字符串 `""` 即可
4. `limit`
   - 表示返回数量上限，范围是 `10 ~ 50`
   - 宽泛检索、只指定一级分类时，建议直接使用 `50`
   - 同时指定一级分类和二级分类，但仍希望尽量多召回时，建议使用 `50`
   - 同时指定一级分类和二级分类，且目标较明确时，建议使用 `25`
   - 同时指定一级分类和二级分类，且检索目标已经非常精确时，可以使用 `10`

---

## 输入格式

```json
{
  "queries": [
    {
      "index0": "过往事件",
      "index1": "owner",
      "memory": "和 owner 有关的约定、承诺、答应过的事",
      "limit": 10
    },
    {
      "index0": "百科知识",
      "index1": "",
      "memory": "和某个作品有关的设定与背景资料",
      "limit": 50
    }
  ]
}
```

---

## 输出格式

```json
{
  "results": [
    {
      "query": {
        "index0": "过往事件",
        "index1": "owner",
        "memory": "和 owner 有关的约定、承诺、答应过的事",
        "limit": 10
      },
      "hints": [
        {
          "id": 12,
          "memory": "......",
          "createdAt": "2026-03-28 21:10:00",
          "msg_ids": [101, 105]
        }
      ]
    }
  ]
}
```

说明：

1. `results` 与输入 `queries` 一一对应，顺序保持一致
2. `query` 是该组 `hints` 对应的原始检索条件
3. `hints` 是命中的长期记忆候选提示，包含以下字段：
   - `id`：长期记忆的唯一标识符
   - `memory`：长期记忆内容
   - `createdAt`：长期记忆创建的时间
   - `msg_ids`：记忆相关的原始消息id列表，可能为空

---

## 结果使用注意事项

1. 检索结果按相关性排序返回，但仍可能包含噪声或弱相关项，需要自行鉴别和取舍
2. 如果命中多条相近结果时，要自行归并和取舍；如果命中结果彼此冲突，要根据实效性、相关性和可靠性进行判断
3. 需要严格证据时，通过 `query-chat-history-skill` 技能追溯原始消息内容

---

## 相关技能

- `query-chat-history-skill`
